<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:name=".other.InternetRequest"
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="几何天气"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/WindowNoTitle">
        <activity android:name=".activity.SystemActivity"></activity>
        <activity android:name=".activity.RegisterActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.LoadActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.ManagerActivity" />
        <activity android:name=".activity.DeleteActivity" />
        <activity android:name=".activity.SearchActivity" />
    </application>

</manifest>